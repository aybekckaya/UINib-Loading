import XCTest
@testable import UINib_Loading

final class UINib_LoadingTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(UINib_Loading().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
